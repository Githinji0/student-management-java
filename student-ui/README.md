# Student UI (JavaFX) - Skeleton

This folder contains a minimal Maven + JavaFX skeleton with blank templates for a Student Management UI.

To build and run (requires JDK 17+ and Maven):

```powershell
cd student-ui
mvn javafx:run
```
